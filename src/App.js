import React, { useEffect } from 'react';

import { Card, Header } from './components';

const App = (props) => {
  return (
    <div className="App">
      <Header />
    </div>
  );
};

export default App;
